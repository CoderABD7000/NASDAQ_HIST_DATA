name: Generate NAS100 Enriched Metrics Dataset (POCs + ADX/DI)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  calculate-metrics:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy

    - name: Run Enriched Metrics Engine
      run: |
        python build_metrics.py

    - name: Commit and Push Processed File
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add NAS100_2019_2026_5m_Enriched_Metrics.csv
        git commit -m "Add standalone NAS100 5m dataset with 3 frozen POCs and ADX/DI metrics" || exit 0
        git push
